<!DOCTYPE html>
<html>
  <head>
    <title>CozyTime Reviews</title>
    <%= stylesheet_link_tag 'application' %>
    <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">
    <%= stylesheet_link_tag 'app' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="page-header">
    <nav class="navbar navbar-default">
      <div class="navbar-header">
        <%= link_to raw("CozyTime Reviews <i class='icon-coffee'></i>"), {controller: "movies", action: "index"}, class: "navbar-brand" %>
      </div>

        <ul class="nav navbar-nav">
          <% if logged_in? %>
            <li><a>Welcome, <i class='icon-user'></i> <%= current_user.name %></a></li>
            <li><%= link_to("Logout", logout_path) %></li>
          <% else %>
            <li><%= link_to("Register", register_path)%></li>
            <li><%= link_to("Login", login_path)%></li>
          <% end %>
            <li><%= form_tag("/search", {method: "get", class: "navbar-form navbar-left"}) do %>
              <%= text_field_tag(:q) %>
              <i class='icon-search'></i>
              <% end %></li>
        </ul>
      </nav>

    </header>
    <%= yield %>
  </body>
</html>
